# BlackWave-Pro - Render Deployment Configuration with Docker + yt-dlp

services:
  - type: web
    name: blackwave-pro
    env: docker
    plan: free
    branch: main
    healthCheckPath: /
    envVars:
      - key: PORT
        value: "10000"
